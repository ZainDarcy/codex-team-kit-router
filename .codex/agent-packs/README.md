# Agent Packs

本目录存放可选行业扩展包。扩展包里的 `.toml` 是模板源，不会默认生效；只有初始化时用户明确选择后，才复制或合并到目标项目的 `.codex/agents/`。

## 规则

- 初始化时必须说明是否启用行业扩展包。
- 未选择的行业扩展包不得复制到 `.codex/agents/`。
- 每个扩展包必须有 `pack.toml` 和 `README.md`。
- 每个扩展 agent 必须保持稳定英文机器名，显示昵称仍由目标项目初始化时随机生成。
- 每个扩展 agent 必须遵守公共文件写锁，不得启动下一层子代理。

